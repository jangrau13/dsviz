{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"dsviz","text":"<p>This is the reference for the language: how to write a program, and what each part of one means. What to build is in the task open in the editor, not here.</p> <p>A program has three parts.</p> <pre><code>@machine\nclass Ledger:\n    held: int = 120\n\n    @duration(0.4)\n    def deposit(amount: int) -&gt; int:\n        held: int = held + amount\n        return held\n\nbank = Ledger(speed=1.0)\n\nworld = World(machines=[bank])\n\ndef story() -&gt; void:\n    now: int = bank.deposit(30)\n\njob = Calls(run=story)\nworld.run(job)\n</code></pre> <p>The machines are decorated classes. A class is a kind of machine; what runs is an instance you make from it, carrying its own speed, its own failure behaviour, and whatever it remembers. A field written in the class body \u2014 <code>held: int = 120</code> \u2014 is state: the next call can see what the last one did, and a crash takes it away again.</p> <p>The world says which machines exist together. Nothing runs outside one, because a machine on its own is a description rather than a system.</p> <p>The job is the computation, handed to the world to run. The same job can be run in a fast world and a broken one, which is the comparison most of the course is about.</p> <p>Everything else is a variation on those three: more machines, a different job, a world that breaks.</p>"},{"location":"language/","title":"Writing a program","text":"<p>One statement per line. <code>#</code> starts a comment. Indentation delimits the body of a function, a loop, a conditional or a parallel block, as in Python.</p> <p>Every name carries a written type. Nothing is inferred: a parameter, a local, a loop variable and a return type are all stated, and the checker holds the program to what was written. That is what lets a job verify a function fits the position it was passed to.</p> <pre><code>def hottest(city: string, readings: [int]) -&gt; int:\n    top: int = 0\n    for reading: int in readings:\n        if reading &gt; top:\n            top: int = reading\n    return top\n</code></pre> <p>Names are yours. A function fits a position because its types fit, and you say which function goes where when you build the job. What you called it never enters into it.</p>"},{"location":"language/#types","title":"Types","text":"Type Is <code>int</code> a whole number <code>string</code> text <code>[int]</code> a list of numbers <code>[string]</code> a list of text <code>pair</code> a (key, value) pair, written <code>(key, value)</code> <code>[pair]</code> a list of pairs \u2014 what a map answers with <code>void</code> nothing <p><code>[int]</code> and <code>[string]</code> are deliberately distinct: the mistakes worth catching are the ones that confuse a list of counts with a list of words.</p> <p>A function that produces one thing returns it. A function that produces an unknown number of them returns a list, and says so: a map is handed one record and answers <code>[pair]</code>, because how many pairs it makes is not known until it has made them. A reduce collapses many values into exactly one, so it answers with that one value.</p>"},{"location":"language/#several-files","title":"Several files","text":"<p>A program can span files. <code>use</code> brings another file's definitions into scope; files are combined in dependency order, and a mistake is reported against the file and line it is in. Circular <code>use</code> and missing files are errors rather than run-time surprises.</p>"},{"location":"language/builtins/","title":"Built-in functions","text":"<p>Deliberately general: <code>split</code> and <code>lower</code> are string operations and <code>sum</code> is arithmetic. Nothing here solves part of a task, because anything problem-shaped is a function you write.</p> Function Type <code>abs</code> <code>abs(n: int) -&gt; int</code> <code>hash</code> <code>hash(key) -&gt; int, a stable 31-hash</code> <code>join</code> <code>join(values: [string], separator: string) -&gt; string</code> <code>len</code> <code>len(x) -&gt; int</code> <code>lower</code> <code>lower(text: string) -&gt; string</code> <code>max</code> <code>max(values: [int]) -&gt; int</code> <code>min</code> <code>min(values: [int]) -&gt; int</code> <code>sort</code> <code>sort(values: [string]) -&gt; [string]</code> <code>split</code> <code>split(text: string) -&gt; [string]</code> <code>strip</code> <code>strip(text: string) -&gt; string</code> <code>sum</code> <code>sum(values: [int]) -&gt; int</code> <code>unique</code> <code>unique(values: [string]) -&gt; [string]</code> <code>upper</code> <code>upper(text: string) -&gt; string</code>"},{"location":"language/checks/","title":"Checks","text":"<p>Statements that assert something about a run.</p>"},{"location":"language/checks/#expect-key-n","title":"<code>expect KEY = N</code>","text":"<p>Assert a final count.</p> <p>The correctness check.</p> <pre><code>expect zurich = 3\n</code></pre>"},{"location":"language/checks/#assert-pclock","title":"<code>assert P.clock == [..]</code>","text":"<p>Check a claimed clock.</p> <p>Write 'assert A || B' for concurrency and 'assert A -&gt;&gt; B' for happens-before. A wrong claim is reported causally.</p> <pre><code>assert P3.clock == [2, 3, 1]\n</code></pre>"},{"location":"language/checks/#note-text","title":"<code>note TEXT</code>","text":"<p>A caption on the diagram.</p> <p>Shown at this point in the run. Useful for narrating a video.</p> <pre><code>note the shuffle starts here\n</code></pre>"},{"location":"language/datasets/","title":"Datasets","text":"<p>Values built from other values, and what is remembered about how.</p>"},{"location":"language/datasets/#textfilename","title":"<code>textFile(name)</code>","text":"<p>Read input into an RDD.</p> <p>The start of a pipeline. The name is an input the program declares or a file the task ships.</p> <pre><code>departures = textFile(\"departures.csv\")\n</code></pre>"},{"location":"language/datasets/#parallelize","title":"<code>parallelize([...])</code>","text":"<p>Make an RDD from a list.</p> <p>Useful when the data is short enough to write down.</p> <pre><code>stops = parallelize([\"bern,4\", \"chur,0\"])\n</code></pre>"},{"location":"language/datasets/#maplambda-x","title":"<code>.map(lambda x: ...)</code>","text":"<p>One record in, one out.</p> <p>Narrow, so it needs no shuffle and pipelines inside the current stage.</p> <pre><code>rows.map(lambda row: row.split(\",\"))\n</code></pre>"},{"location":"language/datasets/#flatmaplambda-x","title":"<code>.flatMap(lambda x: [...])</code>","text":"<p>One record in, many out.</p> <p>Narrow. The function returns a list, and every element of it becomes a record of its own.</p> <pre><code>rows.flatMap(lambda row: row.split(\",\"))\n</code></pre>"},{"location":"language/datasets/#filterlambda-x","title":"<code>.filter(lambda x: ...)</code>","text":"<p>Keep records that match.</p> <p>Narrow, and the cheapest thing you can do before a wide step: every record it drops is one the shuffle does not carry.</p> <pre><code>delays.filter(lambda d: int(d) &gt; 0)\n</code></pre>"},{"location":"language/datasets/#mapvalueslambda-v","title":"<code>.mapValues(lambda v: ...)</code>","text":"<p>Change values, keep keys.</p> <p>Narrow. It leaves the key alone, so nothing has to move.</p> <pre><code>grouped.mapValues(lambda xs: sum(xs) / len(xs))\n</code></pre>"},{"location":"language/datasets/#reducebykeylambda-a-b","title":"<code>.reduceByKey(lambda a, b: ...)</code>","text":"<p>Combine values per key.</p> <p>Wide: it forces a shuffle and begins a new stage. It combines on the map side first, so only one partial result per key crosses the network \u2014 which is what groupByKey does not do.</p> <pre><code>byStop.reduceByKey(lambda a, b: a + b)\n</code></pre>"},{"location":"language/datasets/#groupbykey","title":"<code>.groupByKey()</code>","text":"<p>Gather every value per key.</p> <p>Wide, and it ships every record. reduceByKey reaches the same answer while moving far less, so prefer it when you can.</p> <pre><code>byStop.groupByKey()\n</code></pre>"},{"location":"language/datasets/#sortbykey","title":"<code>.sortByKey()</code>","text":"<p>Order the records by key.</p> <p>Wide: an order across the whole RDD cannot be decided inside one partition.</p> <pre><code>totals.sortByKey()\n</code></pre>"},{"location":"language/datasets/#distinct","title":"<code>.distinct()</code>","text":"<p>Drop repeated records.</p> <p>Wide, because two equal records may sit on different machines.</p> <pre><code>stops.distinct()\n</code></pre>"},{"location":"language/datasets/#joinother","title":"<code>.join(other)</code>","text":"<p>Match two pair RDDs on their keys.</p> <p>Wide: both sides have to be brought together by key.</p> <pre><code>delays.join(platforms)\n</code></pre>"},{"location":"language/datasets/#partitionbylambda-k","title":"<code>.partitionBy(lambda k: ...)</code>","text":"<p>Choose which partition a key goes to.</p> <p>Wide. Deciding the split yourself is how you keep everything that must be compared together on one machine \u2014 and how you cause skew if the function is a poor one.</p> <pre><code>byStop.partitionBy(lambda k: hash(k))\n</code></pre>"},{"location":"language/datasets/#cache","title":"<code>.cache()</code>","text":"<p>Keep this RDD in memory.</p> <p>Without it, an RDD read by two branches is computed twice: the lineage is replayed for each. That is what makes an iterative job expensive.</p> <pre><code>kept = rows.cache()\n</code></pre>"},{"location":"language/datasets/#job-sparkpipelinerdd-loserdd","title":"<code>job = Spark(pipeline=rdd, lose=rdd)</code>","text":"<p>The job to run in the world.</p> <p><code>pipeline</code> names the last step, which is what forces the whole lineage to run. <code>lose</code> throws a step away so you can watch it rebuilt from lineage rather than reloaded.</p> <pre><code>job = Spark(pipeline=totals, lose=byStop)\n</code></pre>"},{"location":"language/functions/","title":"Functions","text":"<p>What you write, and how it is written.</p>"},{"location":"language/functions/#def-nameparam-type-type","title":"<code>def name(param: type) -&gt; type:</code>","text":"<p>A function you write.</p> <p>Every parameter and the return type is written down, and nothing is inferred. Writing them is what lets a job check that a function fits the position it is passed to.</p> <pre><code>def hottest(city: string, readings: [int]) -&gt; int:\n    top: int = 0\n    for reading: int in readings:\n        if reading &gt; top:\n            top: int = reading\n    return top\n</code></pre>"},{"location":"language/functions/#element-for-name-type-in-list","title":"<code>[element for name: type in list]</code>","text":"<p>One element out for each element in.</p> <p>How a function that has to produce many things produces them. A map is handed one record and answers with every pair it made from it, which may be none, one, or thousands \u2014 so it answers with a list, and this is how that list is built.</p> <p>Read it right to left: take each <code>reading</code> out of <code>split(payload)</code>, and for each one put a pair into the list. The loop variable carries its type for the same reason it does in a <code>for</code> statement: nothing here is inferred.</p> <pre><code>def perStation(station: string, payload: string) -&gt; [pair]:\n    return [(station, reading) for reading: string in split(payload)]\n</code></pre>"},{"location":"language/functions/#key-value","title":"<code>(key, value)</code>","text":"<p>The two halves of one intermediate result.</p> <p>What a map answers with a list of. The key decides which partition it goes to, and the value is whatever the reducer takes \u2014 a count, a document name, anything the job is about. A list of them is written <code>[pair]</code>, which is what a map declares as its return type.</p> <pre><code>(station, reading)\n</code></pre>"},{"location":"language/functions/#with-parallel","title":"<code>with parallel():</code>","text":"<p>Calls that all leave at the same time.</p> <p>Calls are one after another: each moves the caller past the whole round trip, so the next one leaves from where the last finished. Everything written inside this block leaves at the moment the block begins instead, and the block ends when the last reply is back \u2014 so asking three machines costs one round trip rather than three. The far end is unchanged, which is the half worth watching: a machine answers one request at a time, so three calls sent at once to the same machine still queue behind each other. What the block saves is the waiting on the wire, never the work \u2014 it pays because the machines are different, not because the calls were written together. No call in the block can be given what another call in it answers, because none of them has answered yet; use the value below the block.</p> <pre><code>def story() -&gt; void:\n    with parallel():\n        here: int = bank.balance(\"savings\")\n        there: int = mirror.balance(\"savings\")\n</code></pre>"},{"location":"language/jobs/","title":"Jobs","text":"<p>Handing your functions to something that runs them.</p>"},{"location":"language/jobs/#job-callsrunf","title":"<code>job = Calls(run=f)</code>","text":"<p>A job that is a sequence of calls.</p> <p>The work is one function, and the job is that function run in the world. Nothing has to be wrapped in a machine to hold it. A MapReduce job has the same shape and takes three functions instead of one.</p> <pre><code>def story() -&gt; void:\n    chf: int = bank.balance(\"savings\")\n\njob = Calls(run=story)\nworld.run(job)\n</code></pre>"},{"location":"language/jobs/#job-mapreducemapf-reduceg-partitionh-partitionsn","title":"<code>job = MapReduce(map=f, reduce=g, partition=h, partitions=N)</code>","text":"<p>Wire your functions into a job.</p> <p>A function is the mapper because it was passed as the mapper, and it is accepted there only if its signature fits. Its name has no say in it. <code>combine=</code> adds a combiner.</p> <p><code>partitions=N</code> says how many ways the keys are split. It is the <code>N</code> your partitioner is handed, and it is not the number of machines: every machine maps, and after the shuffle N of them are each handed a partition to fold. Ask for two partitions in a world of five machines and three of them do no folding at all.</p> <pre><code>job = MapReduce(map=readSensor, reduce=hottest, partition=spread, partitions=2)\n</code></pre>"},{"location":"language/jobs/#job-timesn","title":"<code>Job(..., times=N)</code>","text":"<p>How many rounds the job runs.</p> <p>The job runs N times over. That is what you want in a video, and it is what makes an unreliable run worth watching, because the same call does not fail every time.</p> <pre><code>job = Calls(run=story, times=3)\n</code></pre>"},{"location":"language/machines-you-can-buy/","title":"Machines you can buy","text":"<p>A machine is not built to order. There is a catalogue, you pick a type off it, and the machine arrives with the processor and the room that type comes with \u2014 so making something faster is a purchase rather than a number typed into the program.</p> Type Processor Room For <code>t1.small</code> 0.3x 8 items cheap and slow \u2014 a straggler, and what a job looks like when it is run on whatever was lying around <code>m1.small</code> 1x 16 items the ordinary machine, and what to reach for first <code>m1.large</code> 2x 32 items twice the processor and twice the room <code>c1.large</code> 4x 16 items four times the processor, ordinary room \u2014 for work that is slow rather than large <code>r1.large</code> 1x 96 items ordinary processor, six times the room \u2014 for a partition that will not fit <p>The letter says what the machine is built for. <code>c</code> has the processor, for work that is slow because there is a lot of it to do. <code>r</code> has the room, for a machine handed more than it can hold \u2014 that one does not get better on a quicker processor. <code>m</code> is the middle of both, and <code>t</code> is the cheap one, which is how you are given a straggler.</p> <pre><code>@machine\nclass Worker:\n    pass\n\nfast = Worker(type=\"c1.large\")\nroomy = Worker(type=\"r1.large\")\nordinary = Worker()          # m1.small, if you say nothing\n</code></pre> <p>Each type is drawn in its own colour, so a fleet of mixed machines reads as mixed at a glance.</p> <p>A job that will not fit on one machine can be moved to a bigger one, or made smaller before it is sent. Both are answers. Which is cheaper is the question the catalogue exists to ask, and the panel under the diagram is where the answer shows.</p>"},{"location":"language/machines/","title":"Machines","text":"<p>Declaring a kind of machine, and making ones that exist.</p>"},{"location":"language/machines/#kind","title":"`@kind","text":"<p>class Name:`</p> <p>A kind of machine.</p> <p>The decorator says what the class is to the simulator. @machine both answers calls and makes them, and @process carries a clock. A machine is a machine: which half of a job it does is the job's to decide, the same way a master hands out tasks. A class is only a kind of machine. What runs is an instance of it.</p> <pre><code>@machine\nclass Ledger:\n    @duration(0.4)\n    def balance(account: string) -&gt; int:\n        return 120\n</code></pre>"},{"location":"language/machines/#name-kindspeedn","title":"<code>name = Kind(speed=N)</code>","text":"<p>A machine that exists.</p> <p>Declaring a class runs nothing. Each instance carries its own settings, so two machines of one kind can differ. That is how you make a straggler.</p> <pre><code>fast = Worker(speed=1.0)\nslow = Worker(speed=0.3)\n</code></pre>"},{"location":"language/machines/#field-type-value","title":"<code>field: type = value</code>","text":"<p>What a machine remembers.</p> <p>Written in the class body, above the methods: a name, its type, and the value it starts at. A machine without state answers the same thing however often it is asked; with it, the second call can see what the first one did. Every machine of that kind has its own \u2014 two of them never share a value \u2014 and an instance may start somewhere else by naming the field when it is made. It is drawn along the bottom of the machine on the diagram, and it changes there as the run goes on.</p> <pre><code>@machine\nclass Ledger:\n    balance: int = 120\n\n    @duration(0.4)\n    def deposit(amount: int) -&gt; int:\n        balance: int = balance + amount\n        return balance\n</code></pre>"},{"location":"language/machines/#field-type-expression","title":"<code>field: type = expression</code>","text":"<p>Change what a machine remembers.</p> <p>Inside a method the field is an ordinary name: it reads as what the machine currently holds, and writing to it changes the machine rather than a local that is thrown away when the call returns. Which of the two you get is decided by the class declaration and nothing else, so a parameter may not carry a field's name.</p> <pre><code>@duration(0.4)\ndef deposit(amount: int) -&gt; int:\n    balance: int = balance + amount\n    return balance\n</code></pre>"},{"location":"language/machines/#name-kindfieldvalue","title":"<code>name = Kind(field=value)</code>","text":"<p>Start one machine somewhere else.</p> <p>The class says which fields exist, because that is what makes it this kind of machine; each instance may say what its own start at. That is how two machines of one kind differ in what they hold rather than only in how fast they are.</p> <pre><code>vault = Ledger(balance=5000)\npetty = Ledger(balance=40)\n</code></pre>"},{"location":"language/machines/#kindtypem1small","title":"<code>Kind(type=\"m1.small\")</code>","text":"<p>Which machine to buy.</p> <p>A machine is not built to order: there is a catalogue, you pick a type off it, and the machine arrives with the processor and the room that type comes with. <code>m1.small</code> is the ordinary one and what you get if you say nothing.</p> <p>The letter says what it is built for. <code>c</code> has the processor, for work that is slow because there is a lot of it. <code>r</code> has the room, for a machine handed more than it can hold \u2014 that one does not get better with a faster processor. <code>m</code> is the middle of both and <code>t</code> is the cheap one, which is how you make a straggler.</p> <p>Each type is drawn in its own colour, so a fleet of mixed machines reads as mixed at a glance.</p> <pre><code>  t1.small   0.3x  room 8   cheap and slow\n  m1.small     1x  room 16  the ordinary machine, and what to reach for first\n  m1.large     2x  room 32  twice the processor and twice the room\n  c1.large     4x  room 16  four times the processor, ordinary room\n  r1.large     1x  room 96  ordinary processor, six times the room\n</code></pre> <pre><code>slow = Worker(type=\"t1.small\")\n</code></pre>"},{"location":"language/machines/#durationt","title":"<code>@duration(T)</code>","text":"<p>How long a method takes.</p> <p>Seconds of work at speed 1.0. A machine with speed 0.5 takes twice as long over the same method.</p> <pre><code>@duration(0.4)\ndef balance(account: string) -&gt; int:\n    return 120\n</code></pre>"},{"location":"language/machines/#kinderror_ratep","title":"<code>Kind(error_rate=P)</code>","text":"<p>How likely a machine is to break.</p> <p>0.25 means each piece of work it does has one chance in four of breaking it. The draw is random, so no two runs are alike and one run never settles a question. Every machine runs this risk: a mapper grinding through its split is as exposed as a service answering a request. What happens after it breaks is said separately, with on_crash.</p> <pre><code>flaky = Ledger(speed=1.0, error_rate=0.25)\n</code></pre>"},{"location":"language/machines/#kindon_crashstay_dead-restart","title":"<code>Kind(on_crash=\"stay_dead\" | \"restart\")</code>","text":"<p>What this machine does after it breaks.</p> <p>How often a machine breaks is only half of how it behaves. A machine that stays down and one that is back in two seconds fail at the same rate and behave nothing alike, because retries help the second and are wasted on the first. The default is \"stay_dead\", which leaves it down until something restarts it by hand. Coming back does not bring back what it was holding, so a restarted mapper runs its splits again.</p> <pre><code>flaky = Ledger(error_rate=0.25, on_crash=\"restart\", restart_after=1.5)\n</code></pre>"},{"location":"language/machines/#kindrestart_aftert","title":"<code>Kind(restart_after=T)</code>","text":"<p>How long a restarting machine is down.</p> <p>Seconds between breaking and answering again. It means something only alongside on_crash=\"restart\". The gap shows on the timeline as the machine sitting idle, and that idle time is what you weigh against losing the work outright.</p> <pre><code>slow_to_recover = Worker(error_rate=0.2, on_crash=\"restart\", restart_after=3.0)\n</code></pre>"},{"location":"language/machines/#machinecrash","title":"<code>machine.crash()</code>","text":"<p>Take a machine down.</p> <p>Everything it remembers goes back to the value it started at, whatever it had been counted up to since, and messages already in flight to it are dropped. On the diagram the machine's own values drop back at the moment it breaks, which is the cost of losing it.</p> <pre><code>bank.crash()\n</code></pre>"},{"location":"language/machines/#machinerestart","title":"<code>machine.restart()</code>","text":"<p>Bring a machine back.</p> <p>It comes back as it was declared, not as it was a moment before it broke, so anything it had worked out has to be worked out again.</p> <pre><code>bank.restart()\n</code></pre>"},{"location":"language/machines/#machinemethodarg-deadlinet-retriesn","title":"<code>machine.method(arg [, deadline=T] [, retries=N])</code>","text":"<p>Make a synchronous call.</p> <p>The caller waits for the round trip, so a slow server shows up as caller idle time. Statuses follow gRPC: ok, unavailable, unimplemented, deadline_exceeded.</p> <pre><code>chf: int = bank.balance(\"savings\", deadline=0.5, retries=2)\n</code></pre>"},{"location":"language/messages/","title":"Messages","text":"<p>Processes talking to each other, and what order anyone can be sure of.</p>"},{"location":"language/messages/#sendersendreceiver-label","title":"<code>sender.send(receiver, \"label\")</code>","text":"<p>One message, from one process to one other.</p> <p>The send happens before the receive, and that is the only ordering either process can be sure of. Both carry a logical clock, which the message advances.</p> <pre><code>depotA.send(depotB, \"restock\")\n</code></pre>"},{"location":"language/messages/#senderbroadcastlabel","title":"<code>sender.broadcast(\"label\")</code>","text":"<p>One message, to every other process.</p> <p>One send, one stamp, and a copy on its way to each of the others. Delivery rules that talk about \"the next message that process sent\" are only defined over broadcast, because otherwise the next message may not have been addressed to you.</p> <pre><code>depotA.broadcast(\"restock\")\n</code></pre>"},{"location":"language/messages/#senderbroadcastlabel-latewho","title":"<code>sender.broadcast(\"label\", late=who)</code>","text":"<p>Send one copy the slow way.</p> <p>Everyone else has the message at once; this process does not. Without it every arrival is in send order and nothing is ever out of place, so a delivery rule has nothing to do.</p> <pre><code>depotA.broadcast(\"restock\", late=depotC)\n</code></pre>"},{"location":"language/messages/#callsclockvector-lamport","title":"<code>Calls(clock=\"vector\" | \"lamport\")</code>","text":"<p>Which logical clock the processes keep.</p> <p>A vector clock has one entry per process and can say that two events are concurrent. A Lamport clock is a single number: it guarantees that if a happened before b then L(a) &lt; L(b), and nothing in the other direction.</p> <pre><code>job = Calls(run=deliveries, clock=\"lamport\")\n</code></pre>"},{"location":"language/messages/#callsdeliverycausal","title":"<code>Calls(delivery=\"causal\")</code>","text":"<p>Hold a message until the ones it depends on have arrived.</p> <p>Without this a message is shown when it arrives, however out of order that is. With it, one that arrives too early is held and offered again each time something is delivered. Nothing is dropped and nothing is reordered on the wire \u2014 only the moment each message is shown.</p> <pre><code>job = Calls(run=deliveries, delivery=\"causal\")\n</code></pre>"},{"location":"language/worlds/","title":"Worlds","text":"<p>The machines that exist together, and running in them.</p>"},{"location":"language/worlds/#world-worldmachines","title":"<code>world = World(machines=[...])</code>","text":"<p>The system to run in.</p> <p>Everything about the setting lives here and nothing about the computation, so you can run one job in a fast world and then in a broken one. Without a world, a job has nowhere to run.</p> <pre><code>world = World(machines=[m1, m2, r1])\n</code></pre>"},{"location":"language/worlds/#worldrunjob-on","title":"<code>world.run(job [, on=[...]])</code>","text":"<p>Run a job in the world.</p> <p><code>on</code> gives the job a subsystem instead of the whole world, so you can run one job on three machines and then on ten.</p> <pre><code>world.run(job)\nworld.run(job, on=[m1, r1])\n</code></pre>"}]}