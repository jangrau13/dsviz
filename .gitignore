.venv/
media/
__pycache__/
*.pyc

# dsviz is a library, consumed as a git dependency — each exercise resolves
# and locks for itself. A lock file here pins nothing for anybody and only
# goes stale.
uv.lock
HANDOVER-*.md
